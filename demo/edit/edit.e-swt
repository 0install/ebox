def shell := powerbox.openShell("<untitled>")
shell.addDisposeListener(def _ {
	to widgetDisposed(event) { interp.continueAtTop() }
})

println(`hello!`)
println(`I have: ${<data>.listFiles()}`)

<help:makeHelp>("Edit's Help text")

def main := <this:makeEditor>()

interp.blockAtTop()
